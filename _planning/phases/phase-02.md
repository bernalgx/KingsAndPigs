# Phase 02 — Tileset Integration (placeholder)

Requires Phase 01 `APPROVED`. Planner should rewrite this file once the design bible has been read and the
real tileset image is in the repo — the items below are a starting scaffold from the original transcript,
not a finished spec.

## Expected scope

- Bring Bernal's tileset image into `Assets/Sprites` (or a new `Assets/Tilesets` folder).
- Slice it in Unity's Sprite Editor at PPU 100, matching the rest of the project's art.
- Build the Tile Palette and any Rule Tiles needed for the first biome's terrain.
- Wire it into the existing `Level_Prototype.unity` / a new biome scene.

## Files To Read

- `_planning/current-task.md`
- `.agents/rules/global.md` (Stack → Current Scene State, for the placeholder `GroundPalette` this phase
  replaces)
